from distutils.core import setup

setup(
    name = "googser",
    version = "0.1.0",
    description = "Search google using the command line",
    author = "Dennis Hedback",
    author_email = "d.hedback@gmail.com",
    scripts = ["script/googser"],
)
